---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

`createAccount` and the `api/account-create` handler now accept and propagate `decimalPlaces` so the starting balance is stored using the active currency's precision. Synced accounts continue to assume two decimal places, with a TODO noted in the bank-sync type for follow-up work.
