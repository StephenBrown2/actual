---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

`toRelaxedNumber` now accepts an optional `decimalPlaces` parameter and threads it through `currencyToInteger`/`integerToAmount` so the round-trip is precision-aware for non-2-decimal currencies.
