---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

Transaction rule formulas now scale numeric results using the budget default currency's decimal places via a transient `_decimalPlaces` field. CSV exports and the YNAB-compatible parse-file path now use the active currency precision instead of assuming two decimals.
