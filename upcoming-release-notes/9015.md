---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

Mobile transaction screens (`FocusableAmountInput`, `TransactionEdit`, `TransactionList`, `TransactionListItem`) now use the active currency's decimal places when formatting and parsing amounts. The mobile amount input also flashes an inline error when the user types more decimals than the currency supports.
