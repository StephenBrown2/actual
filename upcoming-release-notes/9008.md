---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

Budget templates honor the active currency's decimal precision: schedule templates pass `_decimalPlaces` to rule actions, the category template overbudget threshold scales with `currency.decimalPlaces`, and template tests now pass an explicit precision to `amountToInteger`.
