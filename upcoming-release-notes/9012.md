---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

`useFormulaExecution`, `useTransactionsSearch`, and the shared `transactionsSearch` query now use the active currency's decimal places when scaling integer amounts and skip amount-equality matches when the user types more decimals than the currency supports.
