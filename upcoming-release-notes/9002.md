---
category: Bugfixes
authors: [StephenBrown2, Cursor]
---

`currencyToInteger` now accepts an optional `decimalPlaces` parameter so callers can convert formatted currency strings into the correct integer minor-unit amount for currencies with non-default decimal places.
